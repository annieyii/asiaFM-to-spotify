[project]
name = "asiafm-to-spotify"
version = "0.1.0"
description = "Scrape Asia FM song history and add tracks to a Spotify playlist"
requires-python = ">=3.10"
dependencies = [
    "python-dotenv>=1.2.2",
    "requests>=2.34.2",
    "spotipy>=2.26.0",
]
